import { AsScrollableDirective } from './as-scrollable.directive';

describe('AsScrollableDirective', () => {
  it('should create an instance', () => {
    const directive = new AsScrollableDirective();
    expect(directive).toBeTruthy();
  });
});
