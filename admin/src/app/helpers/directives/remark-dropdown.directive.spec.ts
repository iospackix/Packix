import { RemarkDropdownDirective } from './remark-dropdown.directive';

describe('RemarkDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new RemarkDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
